# 演示任务的选型和脚本

## 一、演示要证明的是什么

题目要的不是「agent 能点手机」——那个到处都有。要证明的是两件同时成立:

1. agent 真的把一件有用的事做完了
2. 用户那块屏全程是他自己的

第二件事最难演示,因为**「什么都没发生」是看不见的**。所以演示的设计重点是:
让「没被打扰」变成可观察的。做法是让用户在镜头前**持续打字** —— 键盘一旦被抢、
焦点一旦被夺、界面一旦跳走,打字立刻断,观众一眼就看见。反过来,字一路打下去没断,
就是最直接的证据。

## 二、任务选型

### 选中:加购(比价作后备)

**「打开淘宝,搜索 AirPods Pro 2,把搜索结果里第一个商品加入购物车」**

四个候选任务在真机上各跑过多轮,数据说话:

| 任务 | 结果 | 为什么 |
|---|---|---|
| **加购** | ✅ 9 步 32 秒 / 15 步 55 秒,两轮都是干净的 ×1 | 真的改变了外部状态,而且会逼出「看图」那条路(商品详情和购物车整页自绘) |
| 比价 | ✅ 5 步 20 秒 | 稳,但全程只读 —— 证明不了「它真能替你办事」 |
| 备忘录(三星笔记) | ❌ 卡死 | **新建笔记默认是手写模式**,set_text 写不进去,点击只是在画布上画点 |
| 闹钟(三星时钟) | ❌ 卡死 | 「明天早上 9 点」会把它引进日期选择器,而三星闹钟本来就没有日期概念 |

加购比比价强在**它改变了世界**:视频最后一屏是购物车从 (3) 变成 (4),
新商品在最上面 —— 这是可当场核实的物证,而不是一段可以编出来的文字。

**已知风险:淘宝搜索第一名每小时都在变。** 排到「米勒尚品 ¥818」那个时一切顺利;
排到「明阳通海外专营店 ¥1069」那个时,它的加购按钮点了会真的生效却零反馈、
购物车 tab 也进不去,agent 会反复重试。**开录前先手动搜一次看第一名是哪个**,
不对就等一会儿再录,或者当场切到比价那条。

选加购的其余理由:

- **有用**。这是真人真会让助手干的事,不是玩具。
- **多步且有分支**。开 App → 进搜索页 → 填词 → 提交 → 读结果 → 归纳,
  中间任何一步界面不对都得重来,不是「点一两下」。
- **结果可当场核实**。评审拿自己手机搜一遍就能对上,不需要相信我。
- **不可逆操作为零**。不下单、不付款、不给任何人发消息。凡是这类动作,
  agent 一律走通知请示(`ask`),不自己拍板 —— 这条写在系统提示词里。

### 备选(展示「撑得起更复杂的任务」)

- **跨 App 比价**:同一件商品在淘宝和京东各查一次,报哪边便宜。步骤翻倍,
  且要求 agent 在两个 App 之间切换而不丢上下文。
- **长时盯价**:「盯着这个,降到 800 以下叫我」。跑很多轮,每次亮屏检查一次,
  没变化就静默,变了才响。演示视频里放不下,README 里给 trace。

### 淘汰过的

| 任务 | 为什么不用 |
|---|---|
| 「点一杯我常喝的拿铁」 | WhalePhone 自己官网首页就是这个例子,拿它演示等于交白卷 |
| 微信自动回消息 | 要读别人的聊天记录,演示里没法脱敏;而且发消息是不可逆操作 |
| Boss 直聘自动投递 | 场合尴尬 —— 用求职软件给面试官演示 |
| 抢票 / 秒杀 | 涉及支付和真实交易,演示环境不该碰 |

## 三、演示脚本(约 1 分 50 秒)

镜头:**手机自带的屏幕录制**,不接相机也不接电脑。副屏本身是看不见的,但「看副屏」
取景窗已经把它搬到主屏一角,而录屏录的是主屏合成后的画面 —— 悬浮窗在里面(实测确认:
录出来的帧里取景窗和它里面的计算器都在)。所以一段录屏同时包含「用户的界面」和
「agent 在干什么」,不用后期做画中画,也顺带证明了全程不接电脑。

用系统录屏而不是 `adb screenrecord`:前者能录旁白、没有 3 分钟上限、文件直接进相册;
后者停不干净,实测 `kill -INT` 打过去进程不退、mp4 收不了尾,只能靠 `--time-limit`
自然结束,而任务耗时是不定的。

`scripts/demo-record.sh` 负责机器能做的那部分:开录前把 Shizuku、密钥、亮屏、取景窗
四项体检一遍(任何一条不成立,任务都会在你已经开录之后才失败),然后按你的节奏倒计时
发任务、播报任务结束。**打字的人必须是真人**:`input text` 注入的是按键事件,不经过
输入法的组词缓冲,而「正在组词」恰恰是这里最难的一条判据(见 FINDINGS)——
注入录出来的视频看着一样,却没演示到那条路。

| 时间 | 手机主屏(用户) | 取景窗里(agent 的副屏) | 旁白要点 |
|---|---|---|---|
| 0:00 | 人在微信里**已经在打字** | 空 | 演示从用户正在用手机开始,不是从打开我们的 app 开始 |
| 0:05 | 手不离开微信,点一下**悬浮球**,就地展开语音,说出任务 | 空 | 下任务这一步都没离开他的界面:没跳走、没弹键盘、字还在 |
| 0:15 | 接着打字,一直不停 | 淘宝启动,进搜索页 | 从这里起主屏完全是用户的 |
| 0:30 | 继续打字,滑一下信息流 | 填词、提交、点进第一个商品 | 注意用户的输入法没被抢 |
| 0:50 | 还在打字 | 点「加入购物车」,**再自己进购物车核对** | 这一段最值得说:加购按钮点下去零反馈,它不是重点一次,是去能看到结果的地方看一眼(见 FINDINGS) |
| 1:05 | 下拉通知栏 | | 通知静默更新,不弹横幅:「已加入购物车:AirPods PRO3 ¥818,数量 1」 |
| 1:15 | 回到那个 App,**刚才打的字一个不少** | 保温中,副屏还在 | 焦点、键盘、输入内容全程未被动过 |
| 1:25 | **用户自己打开淘宝购物车** | | 物证:(3) 变成 (4),新商品在最上面。这一屏是整段视频的落点 —— 它不是报了个数,是真办了件事 |
| 1:40 | 想追一句就再点悬浮球 | 接着刚才那页往下做 | 上下文默认留着,副屏不重造 —— 追加任务是常态,不该每次冷启动 |

如果想连「它不自己拍板」这条也拍进去,单独补十秒:给它一件明确要花钱或者要给人
发消息的事,它会停下来问;悬浮球变色,对着它回一句「不用」或者「可以」,
它带着原来的上下文接着做 —— 任务是挂起,不是结束。

### 收尾的自检清单

演示最后当场核对这四条,每一条对应一类实测过的冲突:

1. 打字那个 App 的输入框里字完整,中间没有断点 —— 键盘和焦点没被抢
2. 它还停在刚才那一页,没有莫名其妙后退 —— 导航键没打到主屏
3. 长按输入框粘贴,出来的还是演示前复制的内容 —— 剪贴板没被覆盖
4. 最近任务里没有多出用户没开过的界面 —— 副屏销毁时内容没掉到主屏

## 四、演示前的准备

1. **手机刚重启过**:Shizuku 要重新启动一次(见 README),顺便验证开机后
   两个悬浮窗自己恢复了
2. 「悬浮球」打开(用它下任务),取景窗由脚本负责打开,拖到不挡输入框的角上
3. 录音权限给过(悬浮球语音要用),模型接口填好;要演示加购就把 `VLM_MODEL` 也填上
4. 先复制一段文字到剪贴板,收尾时用来核对第 3 条
5. **换一个不介意入镜的聊天窗口**。录屏会把屏幕上的一切都录进去,
   包括联系人和聊天记录 —— 备忘录或者文件传输助手更稳妥
6. 数据线只在跑脚本时需要。想证明「拔了线也一样」,就手动下任务、把线拔掉再录
7. **开录前手动搜一次 AirPods Pro 2,看第一名是哪个商品**。搜索结果是**双列网格**,
   「第一个」指左上角那格。「米勒尚品 ¥818」那个一切顺利;「明阳通海外专营店 ¥1069」
   那个的加购按钮点了会真生效却零反馈、购物车 tab 也进不去,agent 会反复重试。
   不对就等一会儿,或者当场切到比价那条。
   (2026-09-08 01:41 实测:左上角是米勒尚品 ¥818,带「广告」角标、店铺标「逛过的店」——
   它排第一有这台手机的浏览历史在起作用,换台手机未必一样)

7.5 **开录前把淘宝停掉**(`demo-record.sh` 的体检已经会做,除非机主此刻正在用它)。
   App 上一轮被停在哪一页,下一轮 `launch` 回去还是那一页 —— 实测过一次:上一个任务
   停在购物车,这一轮的「搜索」就搜进了**购物车内搜索框**,页面顶上写着
   「您的购物车里没有相关商品」,底下是「合计 ¥0.00 / 结算」,而模型照样从那个降级
   列表里报了个「第一个商品」。连着三次问同一个问题拿到三个不同答案,全是这么来的
8. **购物车里先别有 AirPods**。视频的落点是 (3) 变 (4) 加一件新商品出现在最上面 ——
   已经有一件的话这个变化在画面上看不出来,而且 agent 也会因为「它已经在里面了」而犹豫
9. **语音失败有后备**。识别引擎要连它自己的服务器,断网或者网不稳就会失败(见 FINDINGS)。
   悬浮球上「键盘」那一路是同一个入口,当场切过去就行 —— 视频要证明的是
   「下任务没离开你的界面」,用打字一样成立
10. `bash scripts/demo-record.sh` 的体检会把 Shizuku、模型密钥、**无障碍**(每次装完
   APK 都会被 One UI 关掉)、看图那条路、勿扰逐条查一遍。**开录前先跑一次它**,
   这几条任何一条不成立,任务都会在录像已经开始之后才失败

`DEMO_FEED` 和 `scripts/vd_view.py` 是开发期在电脑上看副屏用的,演示里用不上 ——
手机自己的取景窗直接读那块屏,不落盘、不过线。

## 五、一条要在视频里讲清的边界

agent 的可工作时间等于**用户的亮屏时间**。副屏和主屏共用同一个电源组,主屏一息屏
副屏跟着灭。这不是没做完,是实测出来的设备边界(细节见 FINDINGS.md)。

它和题目是对齐的:要解决的本来就是「用户正在用手机的时候」,那时屏幕必然亮着。
副屏蹭的是已经付过电费的那块屏,手机闲置时 agent 一点电都不耗。
